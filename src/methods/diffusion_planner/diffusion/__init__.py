"""Diffusion utilities for Diffusion Planner.

This package is intentionally minimal (scheduler + timestep embedding + eps loss).
We keep it small but structured so we can later align with the official repo.
"""
