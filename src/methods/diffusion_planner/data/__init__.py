"""Diffusion Planner dataset utilities.

This package contains *minimal* dataset readers used by the new training entrypoint:
`scripts/train/diffusion_planner/train.py`.

We intentionally avoid the legacy `training/` directory in this repo.
"""
