"""Project source package.

This file exists so that modules under `src/` can be imported as a regular
Python package (e.g. `import src.platform...`) when running scripts from the
repository root.
"""
